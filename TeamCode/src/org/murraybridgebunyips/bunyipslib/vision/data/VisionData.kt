package org.murraybridgebunyips.bunyipslib.vision.data

/**
 * Base class for all vision data structures.
 * If used alone, this will act like a [VisionData] with no data, useful for processors
 * that have no meaningful data to provide.
 */
open class VisionData
